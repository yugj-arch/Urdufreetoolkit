# Full-engine deploy for a container host (Hugging Face Spaces, Fly, Render,
# Cloud Run — anywhere without Vercel's 250 MB function limit).
#
# The repo's ../requirements.txt (the local "install everything" list, incl.
# surya/doctr/trocr/rapidocr + fine-tuning tooling) is left untouched. This is a
# leaner subset: the app, the cloud engines, offline transliteration, and the
# two offline OCR engines the picker curates — PaddleOCR + EasyOCR. torch is
# installed CPU-only in the Dockerfile before this file.

# --- app ---
Flask==3.0.3
python-dotenv==1.0.1
Pillow==10.4.0
gunicorn>=21.2

# --- cloud engines ---
openai>=1.40.0
anthropic>=0.40.0
google-genai>=1.0.0
groq>=0.11.0

# --- offline transliteration ---
aksharamukha>=2.2

# --- offline OCR (the curated pair) ---
easyocr
paddleocr
paddlepaddle
opencv-python-headless
Levenshtein
rapidfuzz
